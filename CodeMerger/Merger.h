#pragma once

#include <string>
#include <vector>
#include <memory>

namespace CHTL {

// Placeholder for CodeMerger
class CodeMerger {
public:
    CodeMerger();
    ~CodeMerger() = default;
    
    void merge();
};

} // namespace CHTL