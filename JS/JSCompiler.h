#pragma once

#include <string>
#include <vector>
#include <memory>

namespace CHTL {

// Placeholder for JS Compiler
class JSCompiler {
public:
    JSCompiler();
    ~JSCompiler() = default;
    
    void compile();
};

} // namespace CHTL