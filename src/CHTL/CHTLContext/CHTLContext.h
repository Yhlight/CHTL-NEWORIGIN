#pragma once

#include <string>

namespace CHTL {

    class CHTLContext {
    public:
        CHTLContext();

        // This class will be expanded later to manage the overall compilation state,
        // such as symbol tables, error reporting, configuration, etc.
    private:
        // State variables will be added here.
    };

} // namespace CHTL
